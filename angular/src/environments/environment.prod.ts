//Poner aquí la configuración para que funcione la app en producción, este archivo en
//producción sustituye a la configuración por defecto environments.ts
export const environment = {
  production: true,
  ERROR_LEVEL: 1,
};
